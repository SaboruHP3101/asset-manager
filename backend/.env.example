# Cổng cho API NestJS
PORT=

# Mã secret cho JWT (luồng đăng nhập)
JWT_SECRET=

# Cấu hình riêng lẻ cho Docker
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=

DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}